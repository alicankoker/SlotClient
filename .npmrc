# pnpm configuration
# Enable strict peer dependencies (recommended for monorepos)
strict-peer-dependencies=false

# Auto install peers (useful for development)
auto-install-peers=true

# Shamefully hoist (if needed for compatibility with some tools)
# shamefully-hoist=false

# Store location (default is ~/.pnpm-store)
# store-dir=~/.pnpm-store

