export interface IFreeSpin {
    isActive: boolean; // Indicates if free spins are currently active
    remainingSpins: number; // Number of remaining free spins
    skipAnimations: boolean; // Whether to skip animations during free spins
}